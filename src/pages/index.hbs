<!DOCTYPE html>
<html lang="en">
<head>

    <!--
      This is the main Handlebars template for the site
      - When the user visits the homepage or submits a color the app calls the endpoints in server.js
      - The server script passes data in here and the Handlebars code builds it into the HTML page
    -->

    <meta charset="utf-8"/>
    <link rel="icon" href="https://glitch.com/favicon.ico"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>

    <title>{{seo.title}}</title>

    <!-- Meta tags for SEO and social sharing -->
    <link rel="canonical" href="{{seo.url}}">
    <meta name="description" content="{{seo.description}}">
    <meta property="og:title" content="{{seo.title}}">
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{seo.url}}">
    <meta property="og:description" content="{{seo.description}}">
    <meta property="og:image" content="{{seo.image}}">
    <meta name="twitter:card" content="summary">

    <!-- Import the webpage's stylesheet -->
    <link rel="stylesheet" href="/style.css"/>

</head>
<body>
<div class="wrapper">
    <div class="content" role="main">

        <!-- This is the start of content for our page -->
        <h1 class="title">&nbsp;</h1>


        <div style="display: flex">
            <div class="color-form">
                <form class="color-search" method="post">
                    <label for="amount">
                        Jaká je částka na poukazu?<br>
                        <input id="amount" name="amount" required="required" type="text" value="{{#if amount}}{{amount}}{{else}}1000{{/if}}"><br>
                    </label>

                    <label for="code">
                        Jaký je kód poukazu?<br>
                        <input id="code" name="code" required="required" type="text" value="{{code}}"><br>
                    </label>

                    <label for="language">
                        Jaký je jazyk?<br>
                        <select id="language" name="language" required="required" type="text">
                            <option {{#ifEquals language "cs"}} selected {{/ifEquals}} value="cs">Čeština ▼</option>
                            <option {{#ifEquals language "en"}} selected {{/ifEquals}} value="en">English ▼</option>
                            <option {{#ifEquals language "de"}} selected {{/ifEquals}} value="de">Deutsch ▼</option>
                            <option {{#ifEquals language "pl"}} selected {{/ifEquals}} value="pl">Polski ▼</option>
                            <option {{#ifEquals language "sk"}} selected {{/ifEquals}} value="sk">Slovenčina ▼</option>
                            <option {{#ifEquals language "ua"}} selected {{/ifEquals}} value="ua">Українська ▼</option>
                        </select>
                        <br>
                    </label>


                    <button type="submit">Vygenerovat PDF</button>
                    <a href="{{url}}" download="voucher.pdf" target="_blank" style="opacity: {{#if url}} 1 {{else}} 0 {{/if}}">Stáhnout PDF</a>

                </form>
            </div>
            <div style="background-color: #D0FFF1;  flex: 1; display: flex; flex-direction: column-reverse; align-items: center; justify-content: center;align-content: center">
                {{#if url}}
                    <h1 style="position: absolute">Chystám náhled...</h1>
                    <embed src="{{url}}" width="100%" height="100%" frameBorder="100px" style="z-index: 1234"/>
                {{/if}}
            </div>
        </div>

    </div>
</div>

<div style="position:absolute; right: 20px; bottom: 00px; height: 35px; overflow: hidden">
    <img src="https://static.wikia.nocookie.net/shrek/images/5/56/Humpty_Dumpty.png" width="40px" alt="humpty"/>
</div>
</body>
</html>
