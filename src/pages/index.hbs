<!DOCTYPE html>
<html lang="en">
<head>

    <!--
      This is the main Handlebars template for the site
      - When the user visits the homepage or submits a color the app calls the endpoints in server.js
      - The server script passes data in here and the Handlebars code builds it into the HTML page
    -->

    <meta charset="utf-8"/>
    <link rel="icon" href="https://www.leoexpress.com/static/favicon.ico?v=2"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>

    <title>{{seo.title}}</title>

    <!-- Meta tags for SEO and social sharing -->
    <link rel="canonical" href="{{seo.url}}">
    <meta name="description" content="{{seo.description}}">
    <meta property="og:title" content="{{seo.title}}">
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{seo.url}}">
    <meta property="og:description" content="{{seo.description}}">
    <meta property="og:image" content="{{seo.image}}">
    <meta name="twitter:card" content="summary">

    <!-- Import the webpage's stylesheet -->
    <link rel="stylesheet" href="/style.css"/>

</head>
<body>
<div class="wrapper">
    <div class="content" role="main">

        <!-- This is the start of content for our page -->
        <h1 class="title">&nbsp;</h1>


        <div style="display: flex">
            <div class="color-form">
                <form class="color-search" method="post">
                    <label for="amount">
                        Jaká je částka na poukazu?<br>
                        <input id="amount" name="amount" required="required" type="text" value="{{#if amount}}{{amount}}{{else}}1000{{/if}}" onkeyup="this.value=this.value.replace(/[^\d]/,'')"><br>
                    </label>

                    <label for="code">
                        Jaký je kód poukazu?<br>
                        <input id="code" name="code" required="required" type="text" value="{{code}}" onkeyup="this.value=this.value.replace(/[^\d]/,'')"><br>
                    </label>

                    <label for="language">
                        Jaký je jazyk?<br>
                        <select id="language" name="language" required="required" type="text">
                            <option {{#ifEquals language "cs"}} selected {{/ifEquals}} value="cs">Čeština ▼</option>
                            <option {{#ifEquals language "en"}} selected {{/ifEquals}} value="en">English ▼</option>
                            <option {{#ifEquals language "de"}} selected {{/ifEquals}} value="de">Deutsch ▼</option>
                            <option {{#ifEquals language "pl"}} selected {{/ifEquals}} value="pl">Polski ▼</option>
                            <option {{#ifEquals language "sk"}} selected {{/ifEquals}} value="sk">Slovenčina ▼</option>
                            <option {{#ifEquals language "ua"}} selected {{/ifEquals}} value="ua">Українська ▼</option>
                            <option {{#ifEquals language "hu"}} selected {{/ifEquals}} value="hu">Magyar ▼</option>
                        </select>
                        <br>
                    </label>

                    <label for="classes">
                        Jaká třída?<br>
                        <select id="classes" name="classes" required="required" type="text">
                            <option {{#ifEquals classes "eco"}} selected {{/ifEquals}} value="eco">ECO ▼</option>
                            <option {{#ifEquals classes "eco,ecoplus"}} selected {{/ifEquals}} value="eco,ecoplus">ECO, ECO Plus ▼</option>
                            <option {{#ifEquals classes "eco,ecoplus,bus"}} selected {{/ifEquals}} value="eco,ecoplus,bus">ECO, ECO Plus, BUS ▼</option>
                            <option {{#ifEquals classes "eco,ecoplus,bus,pre"}} selected {{/ifEquals}} value="eco,ecoplus,bus,pre">Vše ▼</option>
                        </select>
                        <br>
                    </label>


                    <button type="submit">Vygenerovat PDF</button>
                    <a href="{{url}}" target="_blank" style="opacity: {{#if url}} 1 {{else}} 0 {{/if}}">Stáhnout PDF</a>

                </form>
            </div>
            <div style="flex: 1; display: flex; flex-direction: column-reverse; align-items: center; justify-content: center;align-content: center; border: 2px solid #000000; border-radius: 4px;">
                {{#if url}}
                    <h1 style="position: absolute">Chystám náhled...</h1>
                    <embed src="{{urlPreview}}" width="100%" height="100%" frameBorder="100px" style="z-index: 1234"/>
                {{else}}
                    <img src="/logo_brutal.png" height="256px" alt="humpty"/>
                {{/if}}
            </div>
        </div>

    </div>
</div>
</body>
</html>
